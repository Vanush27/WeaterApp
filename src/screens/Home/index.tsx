import {View} from 'react-native';
import {BackgroundImageCard, HorizontalCarousel} from '@components';

const Home = () => {
  return (
    <>
      <HorizontalCarousel />
    </>
  );
};

export default Home;
